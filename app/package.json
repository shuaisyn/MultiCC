{
  "name": "webcc-app",
  "version": "1.0.0",
  "description": "WebCC native shell app",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed - using static www/'",
    "cap:sync": "npx cap sync",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
